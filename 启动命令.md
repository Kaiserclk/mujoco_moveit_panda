- 启动mujoco panda机械臂仿真

```
ros2 launch panda_bringup mujoco_panda.launch.py
```

- 初始化位置姿态

```bash
ros2 action send_goal /panda_arm_controller/follow_joint_trajectory control_msgs/action/FollowJointTrajectory "{trajectory: {joint_names: [panda_joint1, panda_joint2, panda_joint3, panda_joint4, panda_joint5, panda_joint6, panda_joint7], points: [{positions: [0.0, -0.785, 0.0, -2.356, 0.0, 1.571, 0.785], time_from_start: {sec: 2}}]}}"
```

- 启动servo_control控制节点

```
ros2 launch panda_bringup keyboard_control.launch.py
```

- 键盘控制节点

```
ros2 run servo_control servo_keyboard_input
```



- 启动手柄节点（如果未启动）

```
ros2 run joy joy_node 
```

- 启动手柄控制节点

```
ros2 run servo_control joy_input_control
```







- 帮我写一个由手柄配合servo_control控制机械臂的节点，程序放在：joy_input_control.cpp中，功能要求如下
- 手柄数据接收由j外部oy_node功能包完成，发布发布话题为/joy
- start按键是手柄的使能按键（程序启动后默认时失能状态，手柄按键按下无效没有响应），使能状态下按下会停止机械臂所有运动，然后手柄失效不能控制
- 通过select按键（button 6）切换不同的控制模式（twist、关节空间），保持可扩展性，后面可能会扩展pose控制
- 关节空间控制模式下：Axis0控制panda_joint1,Axis1控制joint2,Axis3控制joint3,Axis4控制joint4，Axis6控制joint5，Axis7控制joint6，button 1、2控制joint7，button 4、5控制夹爪开合；（Axis6、Axis7是点动轴，单次点击步进，持续按下连续移动，松手停止）、button 1、2、4、5支持长按；只有轴、按键按下时才会移动
- twist控制模式：Axis7控制x轴运动；Axis6控制y轴运动；button 1、2控制z轴运动；Axis0控制x轴旋转，Axis1轴控制y轴旋转，Axis3控制z轴旋转，按键0用于切换命令坐标系在panda_link0、gripper_control_frame之间进行切换

如果有什么问题或者需要补充的向我进行询问





1. **Axis6/Axis7 是点动轴**：标准 joy 消息中 axis 值是 -1 到 1 的浮点数，不是按钮。我需要设置一个阈值（如 0.5）来判断"按下"状态。**阈值设0.7**判断"按下"状态

2. **Button 1/2 控制 joint7**：是 button1 正向、button2 反向吗？是的

3. **Button 4/5 控制夹爪**：是 button4 开、button5 关？还是连续控制（按住开/按住关）？      button4 开，button5 关，按下运动，松手停止

4. **Twist 模式下 button 1/2 控制 Z 轴**：是 button1 正向、button2 反向吗？是的

5. **关节控制模式的速度**：使用固定速度（如 0.5 rad/s）还是可调？   先设置成固定的0.5 rad/s

6. **夹爪控制**：是发布 JointJog 还是有专门的夹爪话题？夹爪关节名是什么？  夹爪控制是通过以下控制器，如果不支持连续控制，可能需要修改/home/kaiser/work-1/mujoco_moveit_panda/panda_bringup/config/ros2_controllers.yaml中控制器类型

   默认夹爪控制器： panda_hand_controller::

     type: position_controllers/GripperActionController